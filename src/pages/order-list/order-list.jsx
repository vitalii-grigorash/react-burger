import styles from './order-list.module.css';

function OrderList() {

    return (
        <section className={styles['order-list']}>
            <h2 className={styles.heading}>Лента заказов вот вот появится</h2>
        </section>
    )
}

export default OrderList;
